import 'package:flutter/material.dart';

class ThirdTitle extends StatelessWidget {
  const ThirdTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Квизометрика',
      style: TextStyle(
        fontSize: 35,
        color: Colors.white,
      ),
    );
  }
}
